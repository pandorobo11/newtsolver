[project]
name = "fmfsolver"
version = "0.1.0"
description = "Sentman free-molecular-flow STL panel solver with PySide6 + PyVistaQt GUI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "numpy",
  "pandas",
  "openpyxl",
  "trimesh",
  "pyvista",
  "pyvistaqt",
  "pyside6==6.9.3",
  "rtree",
]

[project.scripts]
fmfsolver = "fmfsolver.app.gui_app:main"
fmfsolver-gui = "fmfsolver.app.gui_app:main"
fmfsolver-cli = "fmfsolver.app.cli_app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fmfsolver"]
include = ["src/fmfsolver/data/*.csv"]

[tool.hatch.build.targets.sdist]
include = ["src/fmfsolver/data/*.csv"]
